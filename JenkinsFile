pipeline {
    agent {
    kubernetes {
      cloud 'kubernetes'
      label 'docker-slave'
      defaultContainer 'docker-slave'
      }
    }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
